'use client';
import WorkerPlaceholder from '@/components/worker/WorkerPlaceholder';
import { User } from 'lucide-react';
export default function ProfilePage() { return <WorkerPlaceholder title="Identity" icon={User} />; }
