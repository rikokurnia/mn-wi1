'use client';
import WorkerPlaceholder from '@/components/worker/WorkerPlaceholder';
import { Zap } from 'lucide-react';
export default function ActivePage() { return <WorkerPlaceholder title="Active Tasks" icon={Zap} />; }
